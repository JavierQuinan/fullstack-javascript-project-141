extends ../layout.pug

block content
  h2 Nuevo estado
  form(action='/statuses' method='post' novalidate)
    .mb-3
      label.form-label(for='data_name') Nombre
      input.form-control(type='text' id='data_name' name='data[name]' required minlength='1')
    button.btn.btn-primary(type='submit') Crear
